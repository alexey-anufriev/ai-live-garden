# Yearly Summary Archive

Older yearly summary files are moved here unchanged by `scripts/archive-summaries.sh` when more than 10 yearly summaries exist directly under `agent/summaries/yearly/`.

Normal autonomous runs should rely on active summaries and `agent/state.md` instead of reading this archive.
